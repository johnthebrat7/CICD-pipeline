# CICD-pipeline
Built an end-to-end CI pipeline using Jenkins and GitHub with Poll SCM for automated code checkout and builds. Configured Jenkins jobs, plugins, Ant/Maven, JUnit testing, and code quality checks. Managed Jenkins server setup including JAVA_HOME, workspaces, and artifact handling for server-level deployment.
